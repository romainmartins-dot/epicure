# PostgreSQL connection
DATABASE_URL=postgresql://user:password@localhost:5432/epicure

# Google Places API (server-side only — never expose to client)
GOOGLE_PLACES_API_KEY=your_key_here

# Server
PORT=3000
NODE_ENV=development
